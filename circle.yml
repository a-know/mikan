machine:
  timezone: Asia/Tokyo
  ruby:
    version: 2.2.1
test:
  override:
    - bundle exec rspec spec/
deployment:
  feature:
    branch: /^(?!^master$).+$/
    commands:
      - ./scripts/feature_deploy.sh
