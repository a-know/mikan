machine:
  timezone: Asia/Tokyo
  ruby:
    version: 2.2.0
test:
  override:
    - bundle exec rspec spec/
deployment:
  feature:
    branch: /^(?!^master$).+$/
    commands:
      - ./script/feature_deploy.sh
